name: Release extension to marketplace

on:
  pull_request:
    types:
      - closed

jobs:
  release:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Publish
        run: |
          npm install -g vsce
          vsce publish 